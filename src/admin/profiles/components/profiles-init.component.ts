import { Component, ChangeDetectionStrategy } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'sd-profiles-init',
  templateUrl: 'profiles-init.html',
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class ProfilesInitComponent {
}
